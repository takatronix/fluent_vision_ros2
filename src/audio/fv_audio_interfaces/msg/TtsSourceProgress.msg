# Exact lookup result for one end-exclusive prepared-token position.
# Character offsets count Unicode scalar values. UTF-8 offsets count bytes in
# the exact source_text carried by the first speech chunk.
uint64 committed_text_tokens
uint64 source_char_end
uint64 source_utf8_end
